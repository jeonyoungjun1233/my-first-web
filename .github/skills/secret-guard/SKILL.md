# Secret Guard

- API 키, 토큰, 서비스 키는 코드에 직접 하드코딩하지 않는다.
- 환경값이 필요하면 `.env.local`에 저장하고 코드에서는 `process.env`로만 읽는다.
- 민감한 값에는 `NEXT_PUBLIC_` 접두사를 붙이지 않는다.
- 예시 값이 필요하면 실제 비밀값 대신 `your-key-here` 같은 더미 문자열을 사용한다.
- 커밋 전에는 환경변수 노출, 콘솔 출력, 주석에 남은 비밀정보가 없는지 다시 확인한다.
